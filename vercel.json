{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vue",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://rwtogether-backend-你的服务名.koyeb.app/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
